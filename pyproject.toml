[project]
name = "call-me-maybe"
version = "1.0.0"
requires-python = ">=3.10"
dependencies=[
    "numpy>=2.2.6",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.3.1",
]
